enum UnitType {
  area,
  dataTransferRate,
  digitalStorage,
  energy,
  frequency,
  length,
  mass,
  pressure,
  speed,
  temperature,
  time,
  volume,
}

enum ButtonType { numbers, functions, equal }
